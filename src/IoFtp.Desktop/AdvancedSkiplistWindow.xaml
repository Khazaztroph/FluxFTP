<Window x:Class="IoFtp.Desktop.AdvancedSkiplistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Advanced Skiplist" Width="900" Height="540" MinWidth="720" MinHeight="420"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource WindowBrush}" Foreground="{StaticResource TextBrush}">
    <DockPanel Margin="18">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Add" Click="Add_Click"/>
            <Button Content="Remove" Click="Remove_Click"/>
            <Button Content="Up" Click="Up_Click"/>
            <Button Content="Down" Click="Down_Click"/>
            <Button Content="Cancel" IsCancel="True"/>
            <Button Content="Save" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <StackPanel>
                <TextBlock Text="ADVANCED SKIPLIST" FontSize="22" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                <TextBlock Margin="0,5,0,12" Foreground="{StaticResource MutedTextBrush}" Text="Rules are evaluated from top to bottom; the first match applies."/>
            </StackPanel>
            <DataGrid x:Name="RulesGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PATTERN" Binding="{Binding Pattern}" Width="*"/>
                    <DataGridCheckBoxColumn Header="REGEX" Binding="{Binding Regex}" Width="70"/>
                    <DataGridTextColumn Header="TYPE (File/Directory/Both)" Binding="{Binding EntryType}" Width="165"/>
                    <DataGridTextColumn Header="ACTION (Allow/Deny)" Binding="{Binding Action}" Width="145"/>
                    <DataGridTextColumn Header="SCOPE" Binding="{Binding Scope}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock Grid.Row="2" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="{StaticResource MutedTextBrush}"
                       Text="Wildcard patterns support * and ?. Scope can be Allround or Transfer. Advanced rules run before the simple skiplist."/>
        </Grid>
    </DockPanel>
</Window>
